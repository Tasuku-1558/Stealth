#pragma once
#include <fstream>
#include <sstream>
#include <stdio.h>

using namespace std;

namespace StageData
{
	//’è”
	/*const char* STAGE1_CSV_FILE_PATH = "Data/Csv/Stage1.csv";

	FILE* fopen(const char* filename, const char* mode);*/
}